class NeuralNetHolder:

    def __init__(self):
        super().__init__()

        #create network topology using python files
        # load previously saved weights into the network

    def predict(self, input_row):
        # make prediction
#preprocess values
# pas then into the networrk
# denormalise
        return vel_x, vel_y